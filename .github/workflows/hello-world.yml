name: Hello World

on:
   push

jobs:
    hello-job:
        if: github.ref == 'refs/heads/main'
        runs-on: ubuntu-latest
        steps:
            - name: Echo text
              run: echo "sathish"