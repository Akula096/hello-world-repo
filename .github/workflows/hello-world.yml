name: Hello World

on:
   workflow_dispatch:

jobs:
    hello-job:
        concurrency:
            group: prod
            cancel-in-progress: true
        runs-on: ubuntu-latest
        steps:
            - name: Echo text
              run: sleep 300
    